# exotic_bsm

Install the package in editable mode:

```bash
python -m pip install -e .
```

Import the Yukawa calculator with:

```python
from exotic_bsm.exotic_atom_yukawa import ExoticAtomYukawa
```
